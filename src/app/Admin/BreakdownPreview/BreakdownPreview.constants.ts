import { Const } from '@common-ux'

/** Icon theme values for the breakdown email preview — single source for the preview palette. */
export const BreakdownPreviewConstants = {
    icon: {
        chart: { size: 20, color: Const.Color.White[4] },
        section: { size: 16, color: Const.Color.White[5] },
    },
} as const
