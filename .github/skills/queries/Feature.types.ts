// @ts-nocheck — skill example, outside the tsconfig include.

/** Feature-local types. Umbrella names come from `@common-utils`. */
export type FeatureFormData = {
    field1: boolean
    field2: string
    field3: number
    field4: Option<string>
}
