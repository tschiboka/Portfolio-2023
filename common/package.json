{
    "name": "common",
    "version": "1.0.0",
    "private": true,
    "type": "module",
    "description": "Shared domain types, utilities, queries, and reusable UI components (common/ux) used by both the frontend and the server.",
    "dependencies": {
        "moment": "^2.30.1"
    }
}
