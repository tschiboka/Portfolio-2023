import { useCanvas } from './Canvas'
import type {
    Canvas as CanvasType,
    Centre,
    Circle,
    Coord,
    Direction,
    Line,
    Point,
    StyleConfig,
} from './Canvas.types'

export const Canvas = { useCanvas }
export type { CanvasType, Centre, Circle, Coord, Direction, Line, Point, StyleConfig }
